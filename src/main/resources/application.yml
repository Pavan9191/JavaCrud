spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pavandb
    username: root
    password: pk@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQL8Dialect
